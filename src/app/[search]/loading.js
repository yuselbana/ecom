export default function loading() {
    return (
        <div style={{minHeight:'100vh', display:'flex',justifyContent:'center', alignItems:'center'}}>
                <h1 style={{fontSize:'6rem'}}>Fetching Products</h1>
        </div>
    )
}